data base 

insert into produtos(id
                    ,descricao
                    ,valor
                    ,categoria_id) values(default
                                         ,'Refrig. Coca-Cola Lta 350 Ml'
                                         ,5.80
                                         ,1);

create table produtos(id int not null auto_increment
                     ,descricao varchar(50) not null
                     ,valor decimal(8, 2) not null
                     ,categoria_id integer not null
                     ,foreign key (categoria_id) references categorias(id),
                     ,primary key(id)) default charset = utf8;

insert into categorias(id
                      ,nome
                      ,dtcria) values(default
                                     ,'Refrigerantes'
                                     ,current_timestamp),
                                     (default
                                     ,'Salgados'
                                     ,current_timestamp),
                                     (default
                                     ,'Bebidas Alcoolicas'
                                     ,current_timestamp);

create table categorias(id integer not null auto_increment
                       ,nome varchar(50) not null
                       ,dtcria timestamp not null default current_timestamp()
                       ,primary key(id)) default charset = utf8;

user dberpcontainer;

create database dberpcontainer default character set utf8 default COLLATE utf8_general_ci;

show databases;
